/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images: {
  domains: ['localhost', 'starfish-app-xrxoq.ondigitalocean.app'],
}
 ], 
  },
}

module.exports = nextConfig ; 
  

